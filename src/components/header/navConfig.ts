/**
 * Fixed header nav elements that are not part of the live category tree.
 *
 * The Deals entry is a permanent, accent-colored shortcut that always leads the
 * nav row and the mobile drawer — it is a promotional destination, not a
 * catalog category, so it is defined here rather than sourced from the category
 * feed.
 */
export const DEALS_HREF = '/aanbiedingen';
export const DEALS_LABEL = '🔥 Deals';

/** Maximum top-level categories shown inline before the rest collapse under "more". */
export const MAX_INLINE_NAV_ITEMS = 6;
